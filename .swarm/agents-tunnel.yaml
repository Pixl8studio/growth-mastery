# Swarm Agents Configuration (SSH Tunnel Mode)
# Auto-generated for tunnel access
# Use this while Oracle Cloud firewall is being configured

# Agent endpoints (using SSH tunnels)
defaults:
  port: 3847
  status_endpoint: /status
  execute_endpoint: /execute
  health_check_interval: 30s
  timeout: 300s

# Remote agents (via SSH tunnel)
agents:
  - name: swarm-agent-1
    host: localhost
    port: 3847
    status_endpoint: /status
    execute_endpoint: /execute
    enabled: true
  - name: swarm-agent-2
    host: localhost
    port: 3848
    status_endpoint: /status
    execute_endpoint: /execute
    enabled: true

# Metadata
generated_at: 2025-12-05T06:45:00Z
mode: ssh_tunnel
note: "Start tunnels with: ./scripts/swarm/tunnel-agents.sh start"
agent_count: 2
