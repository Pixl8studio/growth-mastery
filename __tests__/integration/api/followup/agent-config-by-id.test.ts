import { describe, it } from "vitest";

// TODO: This route needs implementation at app/api/followup/agent-configs/[configId]/route.ts
describe("GET /api/followup/agent-configs/[configId]", () => {
    it.todo("should get config by id");
    it.todo("should return 401 for unauthenticated users");
    it.todo("should return 404 when config not found");
});

describe("PUT /api/followup/agent-configs/[configId]", () => {
    it.todo("should update config");
    it.todo("should return 401 for unauthenticated users");
});

describe("DELETE /api/followup/agent-configs/[configId]", () => {
    it.todo("should delete config");
    it.todo("should return 401 for unauthenticated users");
});
