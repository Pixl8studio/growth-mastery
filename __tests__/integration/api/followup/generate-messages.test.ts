import { describe, it } from "vitest";

// TODO: This route needs implementation at app/api/followup/sequences/[sequenceId]/generate-messages/route.ts
describe("POST /api/followup/sequences/[sequenceId]/generate-messages", () => {
    it.todo("should generate messages for sequence");
    it.todo("should return 401 for unauthenticated users");
    it.todo("should return 404 when sequence not found");
    it.todo("should return 400 for invalid generation parameters");
});
