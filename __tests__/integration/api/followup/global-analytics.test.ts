import { describe, it } from "vitest";

// TODO: This route needs implementation at app/api/followup/global-analytics/route.ts
describe("GET /api/followup/global-analytics", () => {
    it.todo("should get global analytics across all configs");
    it.todo("should return 401 for unauthenticated users");
    it.todo("should aggregate metrics correctly");
    it.todo("should support comparison periods");
});
