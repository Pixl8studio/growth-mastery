import { describe, it } from "vitest";

// TODO: This route needs implementation at app/api/followup/global-prospects/route.ts
describe("GET /api/followup/global-prospects", () => {
    it.todo("should list all prospects across projects");
    it.todo("should return 401 for unauthenticated users");
    it.todo("should support filtering by status");
    it.todo("should support pagination");
});
